# UniFied Protocol Build System
GOCMD=go
GOBUILD=$(GOCMD) build
BINARY_NODE=build/unified-node
BINARY_CLI=build/unified-cli

all: build

build:
	$(GOBUILD) -o $(BINARY_NODE) ./cmd/node/main.go
	$(GOBUILD) -o $(BINARY_CLI) ./cmd/cli/main.go

test:
	$(GOCMD) test ./...

clean:
	rm -rf build/